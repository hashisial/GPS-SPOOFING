{
  "name": "gps-spoofing-dashboard",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix client",
    "start": "npm run build --prefix client && concurrently -n server,client -c cyan,green \"npm run start --prefix server\" \"npm run preview --prefix client -- --host 0.0.0.0 --port 5173\"",
    "start:client": "npm run preview --prefix client -- --host 0.0.0.0 --port 5173",
    "start:server": "npm run start --prefix server",
    "lint": "npm run lint --prefix server && npm run lint --prefix client",
    "test": "npm run test --prefix server && npm run test --prefix client -- --run"
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}
